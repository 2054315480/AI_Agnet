# 网络问题修复说明

## 问题分析

### 1. 奇怪的"吵架"输出问题
**原因**：系统提示词 `love-advisor.txt` 中包含了恋爱相关的场景描述，包括"争吵"等内容。当使用 `doChatWithTools` 方法时，AI 会基于恋爱顾问的角色来理解用户的请求，导致产生奇怪的联想。

**修复**：在 `doChatWithTools` 方法中添加了工具专用的中性系统提示词，避免恋爱顾问角色干扰工具调用。

### 2. 网络连接超时问题
**原因**：
- `ResourceDownloadTool` 使用 Hutool 的 `HttpUtil.downloadFile()` 方法
- 没有配置超时参数（默认超时时间较长）
- 没有重试机制
- 某些国外网站（如 `file.moyublog.com`）可能需要特殊网络环境

**修复**：
1. 减少超时时间（连接超时 15秒，读取超时 30秒）
2. 添加重试机制（最多重试2次）
3. 改用 `HttpRequest` 显式设置超时和重定向
4. 添加更详细的错误提示和建议
5. 创建 `HttpClientConfig` 配置类支持代理设置

## 修改的文件

### 1. LoveApp.java
- 修改 `doChatWithTools` 方法，添加工具专用系统提示词

### 2. ResourceDownloadTool.java
- 减少超时时间
- 添加重试机制
- 改进 `downloadImage` 和 `fetchUrlContent` 方法
- 添加 URL 格式验证
- 改进错误提示

### 3. LoveAppTest.java
- 优化测试用例
- 移除敏感内容测试
- 添加日志输出
- 添加 `@Slf4j` 注解

### 4. application-local.yml
- 添加 HTTP 客户端配置
- 添加代理配置示例

### 5. HttpClientConfig.java (新文件)
- 支持代理配置
- 支持超时配置

## 后续使用建议

### 如果仍然遇到网络问题：

#### 方案1：启用代理
如果你的网络环境需要代理访问国外网站，可以修改配置文件：

```yaml
http:
  client:
    proxy-enabled: true
    proxy-host: 127.0.0.1  # 你的代理地址
    proxy-port: 7890       # 你的代理端口
```

#### 方案2：使用国内图片来源
在搜索图片时，可以指定使用 Unsplash 等国内可访问的图片源：
```
"搜索 Unsplash 上的免费情侣壁纸"
```

#### 方案3：增加超时时间
如果网络较慢，可以增加超时时间：
```yaml
http:
  client:
    connect-timeout: 30000   # 30秒
    read-timeout: 60000      # 60秒
```

### 测试建议
1. 先测试简单的网页搜索功能（不涉及图片下载）
2. 再测试使用国内可访问的图片资源
3. 如果需要访问国外资源，配置代理后再测试

### 清理测试数据
如果之前的测试数据影响当前测试，可以清理 chat-memory 目录：
```bash
# 备份当前数据
cp -r chat-memory chat-memory-backup

# 清理测试数据
rm chat-memory/*.kryo
```

## 技术细节

### 超时和重试策略
- 连接超时：15秒
- 读取超时：30秒
- 最大重试次数：2次
- 重试延迟：递增延迟（1秒、2秒）

### 错误处理
- URL 格式验证
- HTTP 状态码检查
- 详细的错误提示
- 给出解决建议

### 代理配置
- 支持通过配置文件启用/禁用代理
- 支持配置代理地址和端口
- 自动设置系统代理属性
